services:
  etkg:
    build: .
    container_name: etkg-web
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ETKG_PASSWORD=${ETKG_PASSWORD:-}
