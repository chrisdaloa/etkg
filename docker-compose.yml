services:
  etkg:
    build: .
    container_name: etkg-web
    restart: unless-stopped
    ports:
      - "8888:8000"
    volumes:
      - .:/app
    environment:
      - ETKG_PASSWORD=${ETKG_PASSWORD:-}
